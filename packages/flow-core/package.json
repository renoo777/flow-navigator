{
  "name": "@flow/core",
  "version": "0.1.0",
  "private": true,
  "description": "变量导航器 · 数据模型与核心算法（纯 TS，无 UI 依赖）",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "dagre": "^0.8.5"
  }
}
